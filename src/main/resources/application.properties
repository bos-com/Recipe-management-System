# MongoDB connection. Set environment variable MONGODB_URI to override.
spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/recipe_db}

# Upload directory for images (relative to project root or absolute path). The folder is created at runtime if missing.
app.upload.dir=${UPLOAD_DIR:uploads}

# Thymeleaf cache disabled for development
spring.thymeleaf.cache=false
